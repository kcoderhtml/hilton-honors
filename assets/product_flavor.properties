# Configuration fields values for production flavor can be set here, which will also override those set in the main folder
# However, the default values should already be set with production values in mind, so no values should be needed here